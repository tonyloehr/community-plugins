global:
  scrape_interval: 2s
  evaluation_interval: 2s

scrape_configs:
  - job_name: disposable-authentication-fixture
    honor_labels: true
    static_configs:
      - targets: [metrics:8080]
        labels:
          origin: SIMULATED
