interface:
  display_name: "Audit iOS Readiness"
  short_description: "Run a read-only iOS release preflight"
  default_prompt: "Use $audit-ios-readiness to run a read-only App Store-readiness preflight for the declared iOS target."
policy:
  allow_implicit_invocation: false
