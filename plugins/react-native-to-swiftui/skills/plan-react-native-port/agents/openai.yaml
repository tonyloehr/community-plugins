interface:
  display_name: "Plan React Native Port"
  short_description: "Draft a read-only SwiftUI parity contract"
  default_prompt: "Use $plan-react-native-port to inspect only the declared React Native feature and propose a SwiftUI parity contract. Do not edit files yet."
policy:
  allow_implicit_invocation: false
